# 变量必须以 VITE_ 为前缀才能暴露给外部读取
# NODE_ENV 不用写：vite build 时 Vite 自己就是 production 模式
VITE_APP_TITLE = '硅谷甄选运营平台'
# 接口用相对路径：本机 dev/preview 由 Vite 代理转发，线上由 Nginx 转发到后端容器
VITE_APP_BASE_API = '/api'
# 仅"本机 pnpm preview"用：代理要转给谁（线上是 Nginx 反代，用不到这一行）
VITE_SERVE = 'http://127.0.0.1:10086'
